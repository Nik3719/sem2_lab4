#include <iostream>
#include <thread>
#include "Timer.h"
#include "GenRndChars.h"

using namespace std;





int main(int argc, char const *argv[])
{
    Timer timer;

    // RunMutex(10,10);
    // RunSemaphore(5,10);
    // RunSemaphoreSlim(50,5);
    // RunBarrier(10,10);
    // RunSpinLock(10,10);
    //RunSpinWait(10,10); // работает плохо
    // RunMonitor(10, 10);
    return 0;
}

